#=================================================
# https://github.com/P3TERX/Actions-OpenWrt
# Description: Build OpenWrt using GitHub Actions
# Lisence: MIT
# Author: P3TERX
# Blog: https://p3terx.com
#=================================================

name: OpenWrt-lede6.12-2305

on:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      ssh:
        description: 'SSH connection to Actions'
        required: false
        default: 'false'
  schedule:
    - cron: 0 18 */10 * * 

env:
  REPO_URL: https://github.com/coolsnowwolf/lede
  REPO_BRANCH: master
  FEEDS_CONF: feeds.conf.default
  CONFIG_FILE: 2305.config
  DIY_P1_SH: diy2305.sh
  DIY_P2_SH: diy-part2305-2.sh
  UPLOAD_BIN_DIR: true
  UPLOAD_FIRMWARE: true
  UPLOAD_WETRANSFER: false
  UPLOAD_RELEASE: true
  TZ: Asia/Shanghai
  BUILD_TIME: $(date +"%Y%m%d%H%M")  # 合并统一时间变量

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: 检查项目分支
      uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332  # 固定版本哈希

    - name: 清理磁盘空间
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: true
        swap-storage: true
        
    - name: 安装编译环境（适配 Ubuntu 22.04）
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        df -hT
        sudo -E apt-get -qq update
        # 使用 Ubuntu 22.04 专用依赖列表（P3TERX 官方维护，稳定可靠）
        sudo -E apt-get -qq install $(curl -fsSL https://github.com/P3TERX/Actions-OpenWrt/raw/master/depends-ubuntu-2204)
        sudo -E apt-get -qq autoremove --purge
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone "$TZ"
        sudo mkdir -p /mnt/workdir
        sudo chown $USER:$GROUPS /mnt/workdir
        df -hT

    - name: TG推送（开始编译）
      run: curl "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=🔥X86-openwrt2305自用版固件开刷编译，请耐心等待！"

    - name: 下载固件源码（修复软链接问题）
      working-directory: /mnt/workdir
      run: |
        df -hT $PWD
        git clone $REPO_URL -b $REPO_BRANCH openwrt
        rm -rf $GITHUB_WORKSPACE/openwrt  # 先删除可能存在的旧目录，避免软链接失败
        ln -sf /mnt/workdir/openwrt $GITHUB_WORKSPACE/openwrt
        cd openwrt
        useVersionInfo=$(git show -s --date=short --format="%0AAuthor: %an%0Adate: %cd%0Acommit: %s%0Acommit hash: %H")
        echo "useVersionInfo=$useVersionInfo" >> $GITHUB_ENV
        echo "DATE=$(date "+%Y-%m-%d %H:%M:%S")" >> $GITHUB_ENV   
        echo "FILE_TIME=$(date "+%Y年%m月%d日-%H点")" >> $GITHUB_ENV

    - name: 加载自定义设置
      run: |
        [ -e $FEEDS_CONF ] && mv $FEEDS_CONF openwrt/feeds.conf.default
        chmod +x $DIY_P1_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P1_SH

    - name: 下载插件
      run: cd openwrt && ./scripts/feeds update -a

    - name: 安装插件
      run: cd openwrt && ./scripts/feeds install -a

    - name: 更改设置
      run: |
        [ -e files ] && mv files openwrt/files
        [ -e $CONFIG_FILE ] && mv $CONFIG_FILE openwrt/.config
        chmod +x $DIY_P2_SH
        cd openwrt
        $GITHUB_WORKSPACE/$DIY_P2_SH

    - name: SSH链接管理
      uses: P3TERX/ssh2actions@v1.0.0
      if: (github.event.inputs.ssh == 'true' && github.event.inputs.ssh  != 'false') || contains(github.event.action, 'ssh')
      env:
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}

    - name: 下载安装包
      id: package
      run: |
        cd openwrt
        make defconfig
        make download -j8
        # 清理无效下载文件
        find dl -size -1024c -exec ls -l {} \;
        find dl -size -1024c -exec rm -f {} \;

    - name: 开始编译固件（优化线程数+日志输出）
      id: compile
      run: |
        cd openwrt
        # 限制线程数为核心数的1.5倍，平衡速度和稳定性
        thread_count=$(( $(nproc) * 15 / 10 ))
        echo -e "$thread_count thread compile"
        # 编译日志重定向到文件，方便排查错误
        make -j$thread_count || make -j1 || make -j1 V=s 2>&1 | tee compile.log
        echo "status=success" >> $GITHUB_OUTPUT
        # 获取设备名称
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV || echo "DEVICE_NAME=_X86_64" >> $GITHUB_ENV

    - name: 查看磁盘使用情况
      if: (!cancelled())
      run: df -hT

    - name: 上传bin压缩目录
      uses: actions/upload-artifact@v4.4.0  # 固定版本
      if: steps.compile.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: OpenWrt_bin${{ env.DEVICE_NAME }}_${{ env.BUILD_TIME }}
        path: openwrt/bin/packages/x86_64

    - name: 整理编译好的固件（修复文件丢失问题）
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        # 仅保留有用的固件文件，删除冗余包目录（原逻辑矛盾已修复）
        rm -rf packages  # 删除自动生成的packages目录（非自定义整理的文件）
        # 标记固件目录
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: 上传固件到GitHub Artifacts
      uses: actions/upload-artifact@v4.4.0  # 固定版本
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_2305${{ env.DEVICE_NAME }}_${{ env.BUILD_TIME }}
        path: ${{ env.FIRMWARE }}

    - name: 上传固件到WT网盘
      id: wetransfer
      if: steps.organize.outputs.status == 'success' && env.UPLOAD_WETRANSFER == 'true' && !cancelled()
      run: |
        curl -fsSL https://raw.githubusercontent.com/Mikubill/transfer/master/install.sh | sh
        ./transfer wet -s -p 16 --no-progress ${FIRMWARE} 2>&1 | tee wetransfer.log
        echo "::warning file=wetransfer.com::$(cat wetransfer.log | grep https)"
        echo "url=$(cat wetransfer.log | grep https | cut -f3 -d" ")" >> $GITHUB_OUTPUT
    
    - name: 发布至Release
      id: release
      uses: ncipollo/release-action@v1.14.0
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        name: ${{ env.FILE_TIME }} 「 openwrt-2410-X86-64 」
        allowUpdates: true
        tag: ${{ env.BUILD_TIME }}
        commit: main
        token: ${{ secrets.RELEASE_TOKEN }}
        body: |
          功能介绍：该版本luci为2305，内核版本6.12，目前包含passwall、mosdns、Docker、lucky、微信推送等基础插件，其他功能可以用docker来实现，满足普通用户使用了。
          [固件源码](https://github.com/coolsnowwolf/lede)
          由衷感谢所有为openwrt无私奉献的大佬们
          📥 固件下载
        artifacts: ${{ env.FIRMWARE }}/*

    - name: 清理旧的Workflow运行记录
      uses: Mattraks/delete-workflow-runs@main
      with:
        token: ${{ secrets.RELEASE_TOKEN }}
        retain_days: 180
        keep_minimum_runs: 10

    - name: 删除旧的Releases
      uses: dev-drprasad/delete-older-releases@master
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 9
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.RELEASE_TOKEN }}
        
    - name: TG推送（编译成功）
      if: steps.compile.outputs.status == 'success' && !cancelled()
      run: curl "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=🅰️恭喜主人X86-openwrt2305自用版固件编译完成！🎉%0A⏰编译时间：${{ env.DATE }}%0A📥下载地址：https://github.com/firker/openwrt-Exclusive/releases/tag/${{ env.BUILD_TIME }}"

    - name: TG推送（编译失败）
      if: failure()
      run: curl "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" -d "chat_id=${{ secrets.TELEGRAM_CHAT_ID }}&text=❌X86-openwrt2305自用版固件编译失败！%0A⏰失败时间：${{ env.DATE }}%0A🔍请查看Workflow日志排查问题"

    - name: 上传编译日志（失败时）
      if: failure()
      uses: actions/upload-artifact@v4.4.0
      with:
        name: compile-log_${{ env.BUILD_TIME }}
        path: openwrt/compile.log
